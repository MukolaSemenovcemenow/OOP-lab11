<Window x:Class="CityDirectory.AddEnterpriseWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Enterprise" Height="250" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBox x:Name="NameTextBox" ToolTip="Enter Name" Margin="5"/>
            <TextBox x:Name="AddressTextBox" ToolTip="Enter Address" Margin="5"/>
            <TextBox x:Name="PhoneTextBox" ToolTip="Enter Phone" Margin="5"/>
            <Button Content="Save" Click="Save_Click" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
